# 行健平台 BehaviorOS
# 多模块统一对齐清单
## Cross-Module Alignment Checklist · V1.0 · 2026-03-04
## ✅ Claude Code 本地比对结果 · 2026-03-05

覆盖模块：① 行诊智伴 XZB ② VisionGuard 青少年视力 ③ BehaviorOS H5 ④ vision_food_logger

---

## 零、跨模块冲突速览（优先处理）

| # | 冲突项 | 清单描述 | 实际状态 | 结论 |
|---|--------|---------|---------|------|
| 1 | Celery Job 编号重叠 | XZB Job26-30 vs VisionGuard Job#28 | **Beat schedule 中无 Job26-30**。当前 17 个 beat 任务，按功能名注册（非数字编号）。XZB 专用 Job 尚未加入 beat_schedule。 | ✅ 无冲突（Job 编号仅在规划文档中，未进入代码） |
| 2 | Migration 编号验证 | XZB 053-056 vs Alembic head 059 | **053-056 已执行**（054=xzb_expert_agent 建 10 表，057=embedding_1024_columns）。Head = **059**。 | ✅ 已解决。VisionGuard 应从 **060** 起编 |
| 3 | 向量维度不统一 | XZB=768, 平台不明确, VisionGuard未声明 | xzb_knowledge 有 **双列**：`vector_embedding`=768(旧) + `vector_embedding_1024`=1024(新，migration 057 蓝绿迁移)。Qdrant `bhp_knowledge` = **1024-dim Cosine, 1398 points**。CLAUDE.md 铁律 = **1024**。 | ⚠️ 需统一。**决策：全平台 1024-dim（mxbai-embed-large）**。768 列保留但标记 deprecated |
| 4 | API 路径前缀不一致 | XZB=/api/v1/xzb, VisionGuard=/v1/vision/ | XZB: `/api/v1/xzb/*` (29端点) ✅。Vision: **`/api/v1/vision/*`** (14端点) ✅。两者均使用 `/api/v1/` 前缀。 | ✅ 已解决。代码中 VisionGuard 已统一为 /api/v1/vision/ |
| 5 | Claude API 直调 | vision_food_logger.jsx 直调 api.anthropic.com | **vision_food_logger.jsx 不存在于代码库中**。Vision 模块仅有 `api/vision_api.py` + `core/vision_service.py`。 | ✅ 不适用（前端文件未入库，待开发时按后端代理模式实现） |

---

## 一、行诊智伴（XZB）对齐清单

### 1.1 数据库 / Migration

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| Migration 053-056 执行顺序 | ✅ 已确认 | Alembic head = **059**。053-056 均已执行。054=xzb_expert_agent（10表）, 055=xzb_phase2, 056=xzb_phase3。**无需重编号** |
| xzb_expert_profiles 表：users.id FK 类型 | ✅ 已确认 | users.id = **INTEGER autoincrement**（002_full_schema.py:32）。xzb_expert_profiles.user_id = INTEGER FK → users.id (UNIQUE)。类型一致 |
| xzb_knowledge.vector_embedding = Vector(768) | ✅ 已确认 | 双列策略（migration 057 蓝绿迁移）：`vector_embedding` = Vector(768) 旧列 + `vector_embedding_1024` = Vector(1024) 新列 + HNSW 索引。**平台 Qdrant = 1024-dim**。模型 = **mxbai-embed-large:latest** |
| xzb_med_circle 表（Phase 3） | ✅ 已确认 | **已建表**（migration 054:196-213）。含 `xzb_med_circle_comments` 子表。字段：author_id/title/content/post_type/tags。API 已有 3 端点（GET/POST/POST comment） |
| users 表新增 xzb_expert_id | ✅ 已确认 | users.xzb_expert_id = UUID（migration 054 添加）。无冲突 |
| agent_templates 表新增 xzb_config_id | ⚠️ 待确认 | 未在 migrations 中找到 agent_templates.xzb_config_id 列。需核实是否在 054-056 中以其他名称实现 |
| knowledge_documents.expert_id FK | ✅ 已确认 | expert_id = UUID FK → xzb_expert_profiles.id（migration 054:249）。**NULL 可接受**（现有数据 expert_id 默认 NULL） |

### 1.2 API 接口

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| 路由前缀统一 | ✅ 已统一 | `/api/v1/xzb/*` — 29 个端点。注册于 main.py:719-720。与 VisionGuard `/api/v1/vision/*` 同级。nginx 无需额外配置 |
| 身份验证中间件 | ✅ 已确认 | `get_current_user()` 依赖链完整。XZB 端点使用 `Depends(get_current_user)` 或 expert-specific 验证 |
| POST /api/v1/xzb/experts/register | ✅ 已实现 | 含 license_no 验证。权限守卫需代码 review 确认（xzb_api.py） |
| POST /api/v1/xzb/chat/chat | ⚠️ 路径差异 | 实际路径为 `GET /chat/{conversation_id}/history` + `POST /chat/me/intervene/{conversation_id}` + `POST /chat/me/async-reply/{conversation_id}`。chat_sessions 表 **已存在**（002_full_schema.py:152-165），含 session_id/user_id/model/message_count |
| POST /api/v1/xzb/chat/intervene + /async-reply | ✅ 已实现 | `requires_coach_review` 铁律：XZBRxBridge（core/xzb/xzb_rx_bridge.py:150）中 `requires_coach_review=True` 硬编码。XZBRxFragmentSchema（xzb_models.py:396）默认值 = True |
| POST /api/v1/xzb/rx/manual-trigger | ✅ 已实现 | `POST /rx/trigger/{seeker_id}`。经 XZBRxBridge.process() → 注入 coach_push_queue（source_type='xzb_expert'），非直接推送 |
| POST /api/v1/xzb/knowledge/ingest-document | ✅ 已实现 | 10 个 knowledge 端点。向量化管道使用平台 EmbeddingService。1024-dim 列（vector_embedding_1024）已就绪 |

### 1.3 Celery 调度（Job 26-30）

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| Job 26-30 整体 | ❌ 未实现 | **当前 beat_schedule 共 17 个任务**（13 scheduler + 3 governance + 1 AI recommendation）。XZB Job 26-30 **未注册**，仅存在于规划文档中 |
| Job 26 xzb.knowledge_health_check（02:00） | ❌ 未实现 | 现有 02:00 时段：`safety-daily-report`（scheduler_tasks）。若共存无冲突（不同 key） |
| Job 27 xzb.conversation_digest（03:00） | ❌ 未实现 | 03:00 时段空闲。无冲突 |
| Job 28 编号冲突 | ✅ 不适用 | Beat 任务按 key 名注册（如 `'daily-task-generation'`），**无数字编号系统**。不存在编号冲突 |
| Job 29 xzb.expert_active_check（每小时） | ❌ 未实现 | Redis 锁格式 = `bhp:lock:{name}`，TTL 默认 60s（core/tasks/db.py）。建议 TTL ≤ 3600s |
| Job 30 xzb.knowledge_embedding_sync（04:30） | ❌ 未实现 | 04:30 时段空闲。与 Job 26 (02:00) 不重叠 |

### 1.4 WebSocket 事件

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| XZBWSHandler 注册 | ❌ 未实现 | 当前 3 个 WS 端点（websocket_api.py）：`/ws/content/{id}`, `/ws/feed`, `/ws/user/{user_id}`。无 XZB 专用 handler |
| 5 个新事件类型 | ❌ 未实现 | xzb_expert_message / xzb_expert_online / xzb_knowledge_updated / xzb_rx_pending_review / xzb_knowledge_confirm 均未注册 |
| xzb_rx_pending_review 按角色推送 | ❌ 未实现 | 现有 ConnectionManager 支持按 user_id 推送，但无角色过滤逻辑 |

### 1.5 AgentProxy 集成

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| XZBAgentProxy 接入 | ✅ 已实现 | `core/xzb/xzb_agent_proxy.py`（176行）。process() 5步管道：profile → RAG → LLM → Rx trigger → style transform |
| XZBExpertAgent 注册 | ✅ 已实现 | `core/agents/xzb_expert_agent.py`。Domain=`xzb_expert`, Priority=1, Weight=0.95。已在 startup.py 注册 |
| 注入位置 vs MasterAgent | ✅ 已确认 | MasterAgent = **666行**（core/agents/master_agent.py）。AgentRegistry.freeze() 后 XZBExpertAgent 已在 registry 中。不破坏结构 |
| XZBStyleAdapter + StyleCalibrator | ✅ 已实现 | XZBStyleAdapter（core/xzb/xzb_style_adapter.py, 140行）：5 预设风格（rigorous/warm_guide/tcm_master/evidence_based/lifestyle_coach）。StyleCalibrator 在 `_行智诊疗_review/style_adapter.py:294`（review 目录，未进入主代码） |
| XZBTriggerDetector session_context | ✅ 已确认 | XZBRxBridge.process() 接收 session_context，5 种信号类型（SEMANTIC/DEVICE_DATA/TTM_SHIFT/ADHERENCE/EXPERT_PUSH） |
| XZBRxBridge + RxComposer 兼容 | ⚠️ 部分 | XZBRxBridge（core/xzb/xzb_rx_bridge.py, 210行）→ XZBRxFragmentSchema 格式清晰。RxComposer 在 `_deprecated/` 目录中（已废弃）。**实际 Rx 管道走 BPT-6 RxEngine + coach_push_queue，非旧 RxComposer** |

---

## 二、VisionGuard 青少年视力方案对齐清单

### 2.1 数据库 / Migration（060+）

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| Migration 起始编号 | ✅ 已确认 | Alembic head = 059。057-059 已被占用（057=embedding_1024, 058=059=schema_sync）。**VisionGuard 应从 060 开始** |
| vision_behavior_logs 表 | ⚠️ 待确认 | vision_api.py 存在 14 个端点，但**独立 vision migration 文件未发现**。表可能在 059_schema_sync 中创建，或通过 ORM 自动建表。需核实 DB 中实际表状态 |
| vision_behavior_goals 表 | ⚠️ 同上 | UPSERT 逻辑在 vision_api.py 中实现，PG15 语法兼容 |
| vision_parent_bindings 表 | ⚠️ 同上 | API 端点存在：POST /guardian/bind, DELETE /guardian/{id}。级联策略待核实 |
| 向量维度 | ✅ 已确认 | VisionGuard 继承平台设置 = **1024-dim**（mxbai-embed-large）。无独立声明需求 |

### 2.2 API 接口

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| API 前缀统一 | ✅ 已统一 | **`/api/v1/vision/*`**（vision_api.py, main.py:2378-2379）。14 个端点，与 XZB 同级前缀 |
| GET /api/v1/vision/behavior/logs | ✅ 已实现 | 实际路径：`GET /api/v1/vision/log/me`（个人）+ `GET /api/v1/vision/log/{user_id}`（教练/监护人） |
| POST /api/v1/vision/behavior/checkin | ✅ 已实现 | 实际路径：`POST /api/v1/vision/log`（UPSERT 模式） |
| POST /api/v1/vision/behavior/parent-bind | ✅ 已实现 | 实际路径：`POST /api/v1/vision/guardian/bind` + `DELETE /guardian/{binding_id}` |
| GET /api/v1/vision/behavior/dashboard | ✅ 已实现 | 实际路径：`GET /api/v1/vision/dashboard/{user_id}` |

### 2.3 Agent 扩展

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| VisionGuideAgent | ✅ 已注册 | `core/agents/vision_agent.py`。Domain=`vision`, Priority=3, Weight=0.8, Tier=T2。**5 个意图处理器**：behavior_checkin / goal_inquiry / guardian_summary / resistance_handling / expert_consultation |
| VisionRxAgent | ❌ 不存在 | 未建独立 VisionRxAgent。处方功能由 VisionGuideAgent._handle_expert_consultation() 分流至 XZBRxBridge |
| OphthalmologyExpertAgent | ❌ 不存在 | 需在 xzb_expert_profiles 中预建 ophthalmology 记录。当前无此初始数据 |
| ResistanceAgent | ⚠️ 仅 Legacy | `core/v14/agents.py` 中有 ResistanceAgent（识别：不想/做不到/太难/没用/坚持不了）。**未在主 AgentRegistry 注册**。VisionGuideAgent 自带 `_handle_resistance_handling()`（TTM 感知阻抗应对） |

### 2.4 Celery Job

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| Job#28 冲突 | ✅ 不适用 | Beat 无数字编号系统，按 key 名注册。无冲突。VisionGuard Job 直接用描述性 key（如 `'vision-behavior-rx-trigger'`） |
| vision_behavior_rx_trigger | ❌ 未实现 | 待开发。建议 key = `'vision-behavior-rx-trigger'`，schedule = crontab(hour=23, minute=15) |
| Redis 锁命名 | ✅ 已确认 | 平台锁格式 = `bhp:lock:{name}`（core/tasks/db.py）。Vision 用 `bhp:lock:vision_behavior_rx_trigger` 即可，不冲突 |

### 2.5 前端（visionguard_h5.jsx）

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| 整体 | ❌ 前端未入库 | **visionguard_h5.jsx 不在代码库中**。仅存在于设计文档。后端 API (14端点) 已就绪 |
| 设计系统 | N/A | 独立 H5 入口，与 BehaviorOS H5 互不影响（确认） |
| Mock 数据替换 | N/A | 待前端开发时处理 |
| Google Fonts | N/A | 待前端开发时处理。建议使用本地字体包 |
| 5 Tab 数据源 | N/A | 后端已有对应接口。待前端逐一绑定 |
| 家长绑定入口 | ✅ 后端就绪 | `POST /api/v1/vision/guardian/bind` + `DELETE /guardian/{binding_id}` |

### 2.6 视力营养记录器（vision_food_logger.jsx）

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| 整体 | ❌ 前端未入库 | **vision_food_logger.jsx 不在代码库中** |
| Claude API 直调 | N/A | 待开发时按后端代理模式实现：`POST /api/v1/vision/ai/analyze-food` |
| API Key 暴露 | N/A | Key 将配置于服务器 env var `ANTHROPIC_API_KEY`（已有先例：CLAUDE.md 中 Claude API 配置） |
| 营养素字段 | N/A | vision_behavior_logs 需增加 nutrition_data JSONB 字段（migration 060+） |
| 每周问卷 | N/A | 待建接口：`POST /api/v1/vision/behavior/nutrition-survey` |
| 本地食物数据库 | N/A | 建议接入平台 RAG（Qdrant bhp_knowledge），由 VisionGuideAgent 支撑 |

---

## 三、BehaviorOS H5 原型对齐清单

### 3.1 评估流程数据对齐

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| 入口门类型（door）: symptom/risk/growth | ❌ 后端缺失 | assessment_sessions 表**无 entry_type 字段**（migration 019）。仅有 status/completed_batches/pending_batches。**需新增 entry_type 列**（migration 060+） |
| 体质标签（label）vs BPT-6 | ⚠️ 待映射 | BPT-6 = **6阶段 × 6维度 = 36变体**（core/bpt6_rx_engine.py）。维度 = nutrition/exercise/sleep/emotion/stress/cognitive。前端 label（high_pressure/emotion_drain/night_drain/hidden_fatigue/structural）与 BPT-6 维度可映射但**非直接对应**，需建立映射表 |
| 5维评分 vs BAPS v2.0 | ⚠️ 待确认 | 前端5维（metabolism/stress/sleep/stability/control）与 BPT-6 6维（nutrition/exercise/sleep/emotion/stress/cognitive）有 **3维重叠**（stress/sleep/部分 metabolism≈nutrition），2维需映射（stability→exercise?/control→cognitive?） |
| factormap 雷达图 | ⚠️ 待确认 | 当前前端本地计算。建议上线后改为后端返回以保证可重现性 |
| result 页处方内容 | ❌ 待对接 | 静态 mock。对接路径：`POST /api/v1/assessment-assignments/{assignment_id}/submit` → 返回包含 pipeline_result 的评估结果。**注意：非 /assessment/submit，而是 /assessment-assignments/{id}/submit** |

### 3.2 TTM 阶段与 companion 功能

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| TTM 阶段识别 | ✅ 已实现 | **三层治理架构**（core/stage_authority.py）：StageAuthority → StageEngine（真相源）→ FSM 展示层。决策因素：journey 完成度 + 90天稳定性检查 + JourneyState 标记。输出 S0-S5 |
| companion 屏 AI 对话 | ⚠️ 待确认 | chat_sessions 表已存在（model 字段默认 'qwen2.5:0.5b'）。需确认 session_type = 'behavior_assessment' 是否支持。MasterAgent（666行）支持 chat() 快速对话入口 |
| share 屏参数 | ⚠️ 待确认 | 后端无 entry=share 参数解析逻辑。需在评估入口 API 中增加预填上下文支持 |
| expectation 屏写库 | ❌ 待开发 | assessment_sessions 表无 expectations JSONB 字段。需 migration 新增 |

### 3.3 设计系统与字体

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| 深色/浅色主题隔离 | ✅ 已确认 | BehaviorOS H5 (深色D) 与 VisionGuard (浅色T) 为独立入口 |
| Google Fonts 国内可达 | ⚠️ 待处理 | ZCOOL XiaoWei / Noto Serif SC / Noto Sans SC / Nunito 均需切换国内 CDN 或本地字体包 |
| max-width 390px 适配 | ⚠️ 待测试 | 需在 375px 设备（iPhone SE）上验证 |

---

## 四、跨模块接口契约汇总

### 4.1 API 路径统一规范

| 模块 | API 路径 | 方法 | 实际状态 |
|------|---------|------|---------|
| XZB 专家管理 | /api/v1/xzb/experts/* | GET/POST/PUT | ✅ 已实现（8端点） |
| XZB 知识库 | /api/v1/xzb/knowledge/* | GET/POST/PUT/DELETE | ✅ 已实现（10端点） |
| XZB 对话 | /api/v1/xzb/chat/* | POST/GET | ✅ 已实现（3端点） |
| XZB 处方桥接 | /api/v1/xzb/rx/* | GET/POST | ✅ 已实现（4端点） |
| XZB 医圈 | /api/v1/xzb/med-circle/* | GET/POST | ✅ 已实现（3端点） |
| VisionGuard 打卡 | /api/v1/vision/log | POST | ✅ 已实现 |
| VisionGuard 日志 | /api/v1/vision/log/me | GET | ✅ 已实现 |
| VisionGuard 目标 | /api/v1/vision/goals/me | GET/PUT | ✅ 已实现 |
| VisionGuard 监护人绑定 | /api/v1/vision/guardian/* | POST/GET/DELETE | ✅ 已实现（4端点） |
| VisionGuard 看板 | /api/v1/vision/dashboard/{user_id} | GET | ✅ 已实现 |
| VisionGuard 视力档案 | /api/v1/vision/profile/me | GET/PUT | ✅ 已实现 |
| VisionGuard 检查记录 | /api/v1/vision/exam/* | POST/GET | ✅ 已实现 |
| AI 食物分析代理 | /api/v1/vision/ai/analyze-food | POST | ❌ 待开发 |
| 营养问卷提交 | /api/v1/vision/behavior/nutrition-survey | POST | ❌ 待开发 |
| BehaviorOS 评估提交 | /api/v1/assessment-assignments/{id}/submit | POST | ✅ 已实现 |

**XZB 总端点：29**，**VisionGuard 总端点：14**，**待开发：2**

### 4.2 Celery Job 编号（实际状态）

Beat 任务按 **key 名注册**，无数字编号系统。现有 17 个任务：

| Key | 任务 | 触发时间 | 模块 |
|-----|------|---------|------|
| daily-task-generation | 每日任务生成 | 06:00 | scheduler |
| reminder-check | 提醒检查 | 每60秒 | scheduler |
| expired-task-cleanup | 过期清理 | 23:00 | scheduler |
| process-approved-pushes | 处理已审批推送 | 每5分钟 | scheduler |
| expire-stale-queue-items | 队列过期 | 06:00 | scheduler |
| knowledge-freshness | 知识新鲜度检查 | 07:00 | scheduler |
| program-advance-day | 项目日推进 | 00:00 | scheduler |
| program-push-morning | 晨间推送 | 09:00 | scheduler |
| program-push-noon | 午间推送 | 11:30 | scheduler |
| program-push-evening | 晚间推送 | 17:30 | scheduler |
| program-batch-analysis | 批次分析 | 23:30 | scheduler |
| safety-daily-report | 安全日报 | 02:00 | scheduler |
| agent-metrics-aggregate | Agent 指标聚合 | 01:00 | scheduler |
| governance-health-check | 治理健康检查 | 23:30 | governance |
| coach-challenge-7d-push | 教练7天挑战推送 | 09:00 | governance |
| expert-program-14d-push | 专家14天项目推送 | 00:05 | governance |
| ai-daily-content-recommendation | AI 每日内容推荐 | 08:00 | ai_recommendation |

**XZB 5个 + VisionGuard 1个 待添加**（建议 key 命名：`xzb-knowledge-health-check` / `xzb-conversation-digest` / `xzb-rx-fragment-audit` / `xzb-expert-active-check` / `xzb-knowledge-embedding-sync` / `vision-behavior-rx-trigger`）

---

## 五、工程约定核查

### 5.1 向量维度统一

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| pgvector 维度统一 | ⚠️ 双列过渡中 | **768列**：knowledge_chunks.embedding (TEXT/JSON), xzb_knowledge.vector_embedding (Vector(768))。**1024列**：knowledge_chunks.embedding_1024 (TEXT/JSON), xzb_knowledge.vector_embedding_1024 (Vector(1024), HNSW 索引)。Migration 057 已建双列。 |
| **最终决策** | ✅ **1024-dim** | 理由：Qdrant=1024 / CLAUDE.md铁律=1024 / mxbai-embed-large=1024 / 已入库1398点=1024。768列标记deprecated，新数据仅写1024列 |
| VisionGuard 知识向量维度 | ✅ 已确认 | 继承平台 = 1024。无需独立声明 |
| 嵌入同步 Job 统一 | ❌ 待确认 | XZB Job 30 = xzb.knowledge_embedding_sync（待实现）。VisionGuard 知识块建议共用平台 embedding 管道，不新建独立 Job |

### 5.2 Redis 锁命名规范

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| 锁 key 格式 | ✅ 已确认 | 统一格式 = `bhp:lock:{name}`（core/tasks/db.py）。TTL 默认 60s，支持自定义。atomic SET NX EX |
| XZB 锁 key | ✅ 无冲突 | 建议：`bhp:lock:xzb_knowledge_health_check` / `bhp:lock:xzb_expert_active_check` 等 |
| VisionGuard 锁 key | ✅ 无冲突 | 建议：`bhp:lock:vision_behavior_rx_trigger` |

### 5.3 处方铁律（全模块共享）

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| requires_coach_review = True | ✅ 已确认 | XZBRxFragmentSchema（xzb_models.py:396）：`requires_coach_review: bool = True`。XZBRxBridge.process() 注入 coach_push_queue 时强制 True（xzb_rx_bridge.py:150） |
| coach_push_queue 唯一入口 | ✅ 已确认 | 表在 coach_schema（migration 038 迁移）。source_type = VARCHAR(30)，已知值：`challenge` / `device_alert` / `micro_action` / `ai_recommendation` / `system` / **`xzb_expert`**。VisionGuard 处方需新增 source_type = `vision_rx` |
| 处方推送上限 3条/用户/天 | ⚠️ 模板级 | `pushes_per_day INTEGER DEFAULT 3`（migration 045:53）在 program_templates 表。**非 API 网关级统一限流**。若需全局限制，需在推送网关层（process-approved-pushes task）增加计数逻辑 |

### 5.4 小程序部署约定

| 清单项目 | 状态 | 实际值 / 备注 |
|---------|------|-------------|
| Google Fonts 国内可达 | ⚠️ 待处理 | 所有前端（BehaviorOS H5 + VisionGuard H5 + vision_food_logger）均引用 Google Fonts。需统一切换国内 CDN（如 fonts.loli.net）或打包本地字体 |
| Claude API 代理接口 | ❌ 待开发 | `POST /api/v1/vision/ai/analyze-food`。域名需加入小程序 request 合法域名白名单 |
| subpackage 分包策略 | ⚠️ 待确认 | VisionGuard 和 vision_food_logger 应独立分包。当前 coach-miniprogram 已有多个 subpackage（pages/coach/, pages/learning/ 等） |

---

## 六、执行优先级（更新后）

| 优先级 | 清单项目 | 实际状态 |
|--------|---------|---------|
| **P0 立即** | ① Celery Job 编号 | ✅ **无冲突**（按 key 名注册，非数字编号） |
| **P0 立即** | ② API 前缀统一 | ✅ **已统一**（XZB=/api/v1/xzb, Vision=/api/v1/vision） |
| **P0 立即** | ③ 向量维度统一 | ⚠️ **决策已定=1024，蓝绿双列过渡中**。768列标 deprecated |
| **P0 立即** | ④ Migration 编号核实 | ✅ **已确认**。Head=059，VisionGuard 从 060 起编 |
| **P1 本周** | ⑤ vision_food_logger 后端代理 | ❌ 前端未入库，待开发 `POST /api/v1/vision/ai/analyze-food` |
| **P2 两周** | ⑥ BehaviorOS H5 枚举对齐 | ❌ assessment_sessions 缺 entry_type 列；BPT-6 映射表待建 |
| **P2 两周** | ⑦ VisionGuard Mock→真实API | ❌ 前端未入库。后端 14 端点已就绪 |
| **P3 上线前** | ⑧ Google Fonts 替换 | ⚠️ 待处理 |

---

## 附录：关键发现汇总

### 已实现但清单未覆盖的能力
1. **XZB 医圈**（med-circle）3 端点已实现，清单未列入
2. **Vision 视力档案**（profile/exam）5 端点已实现，超出清单范围
3. **XZBStyleAdapter** 5 预设风格已实现（rigorous/warm_guide/tcm_master/evidence_based/lifestyle_coach）
4. **VisionGuideAgent** 5 意图处理器已实现（behavior_checkin/goal_inquiry/guardian_summary/resistance_handling/expert_consultation）

### 需要注意的差异
1. **RxComposer 已废弃** → 实际 Rx 管道 = BPT-6 RxEngine + coach_push_queue
2. **ResistanceAgent 仅在 Legacy v14** → VisionGuideAgent 自带阻抗处理
3. **Beat 任务无数字编号** → Job 26-30 概念仅存在于规划文档
4. **assessment 提交路径** = `/api/v1/assessment-assignments/{id}/submit`（非 `/assessment/submit`）
5. **处方限流**在 program_templates 表（模板级），非 API 网关级

---

> 文档版本：V1.0-FILLED
> 比对日期：2026-03-05
> 比对方式：Claude Code 本地三路并行调查（DB/Migration + API/Celery + Agent/Rx）
> 代码库分支：stabilize-from-sprint1, HEAD = latest
