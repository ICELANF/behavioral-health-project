修改 src/pages/coach/index/index.vue（教练首页工作台）：

后端 GET /v1/coach/dashboard 返回结构：
{
  coach: { id, name, level },
  today_stats: {
    total_students: 4,
    pending_followups: 4,
    completed_followups: 0,
    alert_students: 4,
    unread_messages: 0
  },
  students: [...]
}

请更新首页顶部统计区域的字段映射：
- 学员数 → today_stats.total_students
- 高风险 → today_stats.alert_students  
- 待审批 → 保持原有 coach-push/pending 接口（返回4条）
- 待评审 → today_stats.pending_followups

同样修改 src/pages/coach/dashboard.vue，对齐相同数据结构。

完成后 npm run build:mp-weixin