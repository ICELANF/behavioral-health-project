请实现 Coach 飞轮完整功能：

1. src/pages/coach/flywheel/index.vue — 飞轮审核主页

布局设计：
- 顶部：今日统计横排4格
  · 待审核（pending，橙色）
  · 已审核（reviewed，蓝色）  
  · 已通过（approved，绿色）
  · 已退回（rejected，红色）
  · 调用 GET /v1/coach/stats/today

- 中间列表：待审核队列
  · 调用 GET /v1/coach/review-queue
  · 每张卡片显示：
    - 学员姓名 + 风险标签（R1-R4色块）
    - 类型标签（rx_push/prescription/assessment）
    - AI摘要文字（item.ai_summary，灰色小字）
    - AI草稿内容（item.ai_draft，主体文字，可折叠）
    - 底部两个按钮：「✓ 通过」（绿色）和「✗ 退回」（红色）
    - 通过 → POST /v1/coach/review/:id/approve
    - 退回 → 弹窗输入原因 → POST /v1/coach/review/:id/reject

- 通过后：卡片变灰+显示「已通过 ✓」，刷新今日统计
- 退回后：卡片变灰+显示「已退回」，刷新今日统计
- 空状态：绿色✓图标 + 「今日审核已全部完成！」

2. 在 src/pages/coach/index/index.vue 工作台入口区：
   在「数据分析」后新增「AI飞轮」入口
   图标：🤖，标签：AI飞轮
   点击跳转 /pages/coach/flywheel/index

3. 在 src/api/coach.ts 新增：
   - getFlywheelQueue() → GET /v1/coach/review-queue
   - getFlywheelStats() → GET /v1/coach/stats/today
   - approveReview(id) → POST /v1/coach/review/:id/approve
   - rejectReview(id, reason) → POST /v1/coach/review/:id/reject

完成后 npm run build:mp-weixin