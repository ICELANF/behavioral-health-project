检查 src/pages/notifications/index.vue 文件内容，
如果还是占位符（包含"页面开发中"），
说明上批实现的是错误的文件路径。

请重新实现 src/pages/notifications/index.vue，
实现消息通知页：
- 顶部 Tab：全部/系统/学习/教练
- 通知列表（未读红点 + 标题 + 摘要 + 时间）
- 顶部「全部已读」按钮
- 空状态显示「暂无消息」
- 调用 GET /v1/notifications?type=all

同时检查 src/pages/profile/index.vue 是否也是占位符，
如果是也一并重新实现：
- 头像 + 姓名 + 等级徽章
- 三维积分小卡片
- 功能菜单列表（学习记录/评估/考试/晋级/证书/排行/设置）
- 退出登录按钮
- 调用 GET /v1/users/me

完成后执行 npm run build:mp-weixin。