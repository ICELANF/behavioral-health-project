docker exec bhp_v3_api python -c "
import sys; sys.path.insert(0, '.')
from app.database import get_db
from app.models import User
db = next(get_db())
users = db.query(User).all()
for u in users:
    print(f'{u.username:20} role={u.role}  active={u.is_active}')
"