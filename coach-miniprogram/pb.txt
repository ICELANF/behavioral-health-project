1. 修改 src/pages/coach/assessment/index.vue：
   找到量表选项列表，把 SCL-90/PHQ-9/GAD-7/PSQI/SF-36/BFI-44 
   替换为后端支持的量表：
   - ttm7（行为改变阶段）
   - big5（大五人格）
   - bpt6（行为类型）
   - capacity（能力评估）
   - spi（健康行为指数）
   显示名称用中文：TTM行为阶段/大五人格/行为类型BPT6/能力评估/健康行为指数
   
2. 修改 src/pages/coach/flywheel/index.vue：
   找到「一键生成跟进计划」调用 agent/run 的地方，
   确认传参格式：
   { agent_type: 'COACHING', user_id: String(studentId), input: '生成个性化跟进计划' }
   agent/run 已验证返回200，如果还是422说明前端studentId未正确获取，
   加一行 console.log('runAgent studentId:', studentId) 调试

完成后 npm run build:mp-weixin