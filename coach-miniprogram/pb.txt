docker exec bhp_v3_api python -c "
import sys; sys.path.insert(0, '.')
from app.database import get_db
import sqlalchemy as sa
db = next(get_db())
inspector = sa.inspect(db.bind)
tables = inspector.get_table_names()
content_tables = [t for t in tables if any(k in t for k in ['content', 'course', 'video', 'article', 'task', 'recommend'])]
print(content_tables)
for t in content_tables[:3]:
    cols = [c['name'] for c in inspector.get_columns(t)]
    print(f'{t}: {cols}')
" 2>/dev/null